# Next steps

Generated by `/next` on 2026-07-19. Based on codebase analysis and documentation sync.

## Doc sync

- `CLAUDE.md`: Replaced stale monolithic-`main.rs` architecture with current `ui/` / `core/` / `services` layout; noted `make test` and real `config.yml` / `ui_state` keys.
- `ARCHITECTURE.md`: Documented `favorites_only` in per-folder `ui_state`.
- `README.md`: Documented cut/move and existing accel / fullscreen shortcuts; fixed `ui_state` key list; marked version checker roadmap item done (URL already points at `mholtzhausen/lumen-node`).

## Low Hanging Fruit

1. **Richer search placeholder** — Users see `Search...` and may assume filename-only; search already matches metadata (`ui/models.rs`, `shell.rs`). Effort: S. How: Set placeholder to e.g. “Search filename or prompt…” on `search_entry`.
2. **F2 rename** — Hover rename chrome exists (`ui/grid.rs` / `dialogs::open_rename_dialog`) but no keyboard path. Effort: S. How: In `install_keyboard_handler`, map F2 (when not in a text field) to the existing rename dialog for the selection.
3. **Wire Copy Seed accelerator** — Sibling copy actions have Ctrl+Shift shortcuts; `ctx.copy-seed` has none (`register_context_menu_accels` in `ui/actions.rs`). Effort: S. How: Add `<Primary><Shift>s` (or similar) next to the other `set_accels_for_action` calls.

## UI/UX Upgrades

1. **Empty / zero-result states** — Empty grid when no folder, no images, or filters exclude everything is a blank `GridView` with no guidance. Effort: M. How: Overlay an `adw::StatusPage` (open folder / clear filters / no matches) driven by selection model + filter state.
2. **In-app shortcuts cheat sheet** — Power shortcuts live in code and README but not in the UI (`ui/shell.rs` menus, `register_context_menu_accels`). Effort: M. How: Add Help → Keyboard Shortcuts (`GtkShortcutsWindow`) listing navigation, clipboard, AI copy, and refresh accels.
3. **Favorites-filter feedback** — Favorites-only can empty the grid with no explanation (`install_favorites_only_handler` in `ui/controls.rs`). Effort: S. How: When active and `n_items == 0`, show a short StatusPage or toast hint to turn the filter off.

## Bugfixes

1. **Cut/move fails across filesystems with a misleading toast** — Ctrl+X then Ctrl+V uses only `std::fs::rename`; on failure the cut path is already `take()`n and the code falls through to texture paste, toasting “Clipboard does not contain an image” (`ui/keyboard.rs` ~99–127). Effort: M. How: On rename failure, `fs::copy` + remove (or restore cut path and toast “Could not move”); only then fall back to image paste.
2. **Cross-folder cut leaves stale DB rows in the source folder** — On successful move into the open folder, `remove_image_row` only runs when `source.parent() == current folder` (`ui/keyboard.rs`); cutting from another directory never opens that folder’s `.lumen-node.db`. Effort: S. How: If `source.parent()` differs, open that DB and remove the old path (or prune on next visit).
3. **ComfyUI pos/neg can swap when negative text is longer** — `extract_comfyui_prompts` picks longest / second-longest `CLIPTextEncode` strings (`metadata.rs`), so verbose negatives invert roles. Effort: M. How: Prefer graph/link heuristics (e.g. title/`negative` path scoring already used in workflow walk) before length sort for API `"prompt"` JSON.

## Features

1. **Side-by-side compare** — Core niche workflow for picking between AI gens / photo takes; already on the product roadmap in README. Effort: L. How: Pin a reference image, split center into two `Picture`s with shared next/prev or lock-left navigation.
2. **Free-form tags (beyond favourite)** — Favourite is binary (`db` `favourite` column); AI libraries need “keep / archive / style / model” labels. Effort: L. How: Add a tags table or JSON column in `.lumen-node.db`, filter chips in the header, and search inclusion.
3. **Prompt / parameter similarity browse** — Differentiator vs generic galleries: use cached `prompt` / `raw_parameters` to surface near-duplicates or same-seed variants. Effort: L. How: After enrichment, index normalized prompt tokens (or hash of prompt+seed) and offer “Similar in folder” from the context menu / metadata pane.
