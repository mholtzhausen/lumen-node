# Next steps

Generated by `/next` on 2026-07-20. Based on codebase analysis and documentation sync.

## Doc sync

- `README.md`: Documented cross-filesystem cut/move (copy+remove); added `Ctrl+,` / `Ctrl+?`; listed `similarity.rs` and newer `ui/` modules in the architecture tree.
- `CLAUDE.md`: Documented `similarity.rs`, zoom/preferences/empty_state/shortcuts in `ui/`, and scan_coordinator clearing of similarity state.

## Low Hanging Fruit

1. **Ctrl+O for Open Folder** — Standard “open” accel is missing even though `win.open-folder` exists. Evidence: `ui/shell.rs` (`win.open-folder`), no matching `set_accels_for_action`. Effort: S. How: Register `<Primary>o` next to prefs/shortcuts accels and add a Shortcuts window row.
2. **Accels for Pin / Add tag / Similar** — Organise power features are context-menu-only. Evidence: `ctx.pin-for-compare`, `ctx.add-tag`, `ctx.show-similar` in `ui/actions.rs`; `register_context_menu_accels` only wires copy/refresh. Effort: S. How: Add sensible Primary+Shift (or Primary+Alt) bindings and list them in `ui/shortcuts.rs`.
3. **Grid trash tooltip says “Delete file”** — Hover chrome calls `open_trash_dialog` but the tooltip implies permanent delete. Evidence: `ui/grid.rs` (`set_tooltip_text(Some("Delete file"))`). Effort: S. How: Change tooltip (and optionally icon tooltip) to “Move to Trash”.



## UI/UX Upgrades

1. **Reset immersive views on folder change** — Switching folders clears the compare pin and caches but can leave `ViewStack` on `single`/`compare` with stale pictures. Evidence: `core/scan_coordinator.rs` clears pin/index only; no `set_default_grid_page` / `clear_picture`. Effort: M. How: On scan start, force grid page and clear single/compare/meta pictures.
2. **Similar-filter header chrome** — Favourites/tags get active CSS; similar-only filtering is toast + × with no persistent indicator. Evidence: `controls.rs` `favorites-filter-active` / `tags-filter-active`; `similar_paths` has no peer class. Effort: S. How: Add a header badge/CSS when `similar_paths` is `Some`, and a clear control that only drops similar.
3. **Clear (×) should not reset sort** — Header clear forces name ascending, surprising when the user only wanted to drop search/filters. Evidence: `apply_clear_filters` in `ui/controls.rs` sets `SORT_KEY_NAME_ASC` and `sort_dropdown.set_selected(0)`. Effort: S. How: Clear search, favourites, tags, and similar only; leave `sort_key` alone.



## Bugfixes

1. **Rename/cut drop favourite (cut also drops tags)** — Path moves re-index the destination without migrating `favourite`; cut never calls `move_tags`. Evidence: `dialogs.rs` rename → `move_tags` + `remove_image_row` + `refresh_indexed`; `favourite_for_path` in `db.rs` looks up the new path (miss → 0); cut path in `ui/keyboard.rs` removes source then `refresh_indexed` destination. Effort: M. How: Add `move_image_row` (or preserve favourite + tags before remove) and use it from rename and cut.
2. **Similarity index goes stale after rename/cut** — `replace_path` removes the old prompt-index entry; `insert_path` never upserts. Evidence: `ui/list_mutation.rs` `remove_path` / `insert_path`. Effort: S. How: After insert/replace, upsert from `meta_cache` (or re-enrich) into `prompt_similarity_index`.
3. **About dialog points at the wrong GitHub URL** — In-app About website is `nemesarial/lumen-node` while updater/README/metainfo use `mholtzhausen/lumen-node`. Evidence: `ui/shell.rs` `set_website`; `updater.rs` releases API. Effort: S. How: Point About (and authors if desired) at the canonical repo URL.



## Features

1. **Duplicate-by-hash browse** — SHA-256 is already stored; prompt-similar exists but byte-identical duplicates have no UI. Evidence: `db.rs` `hash` column / `hash_file`; `similarity.rs` is prompt-only. Effort: M. How: Group by hash in-folder and offer “Duplicates in folder” from context menu / empty-state when count > 1.
2. **Multi-select + bulk organise** — Cull/tag workflows need selecting many images at once. Evidence: `ui/models.rs` builds `SingleSelection` only. Effort: L. How: Switch to `MultiSelection` (or Ctrl/Shift range), then bulk favourite / tag / trash with shared dialogs. How to deal with preview pane during multi-select?
3. **Optional include-subfolders scan** — Tree browse is one level deep; large AI dumps often nest by date/model. Evidence: `scanner.rs` immediate `read_dir` only (non-recursive). Effort: L. How: Preference or Open-dialog toggle for recursive enumerate with depth/safety caps, still writing one DB at the chosen root.

